openapi: 3.0.3
info: { title: BPMappr API, version: 0.1.0 }
paths:
  /health:
    get: { responses: { "200": { description: OK } } }
  /ingest:
    post: { summary: Upload de artefato, responses: { "200": { description: OK } } }
  /generate:
    post: { summary: Geração BPMN a partir de artifacts, responses: { "200": { description: OK } } }
  /edit:
    post: { summary: Patch por comando natural, responses: { "200": { description: OK } } }
  /export:
    get: { summary: Exporta .bpmn/.png/.json, responses: { "200": { description: OK } } }
